# Point-in-time recovery after switching to Postgres.
# Add to postgresql.conf, then restart.

wal_level = replica
archive_mode = on
archive_command = 'test ! -f /var/lib/postgresql/wal_archive/%f && cp %p /var/lib/postgresql/wal_archive/%f'
archive_timeout = 300